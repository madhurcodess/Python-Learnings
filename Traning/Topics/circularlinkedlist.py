class Circular:
    def __init__(self):
        self.head = None

    def insert_at_beginning(self, data):
        new_node = Node(data)

        if self.head is None:
            self.head = new_node
            new_node.next = self.head
            return

        last = self.head
        while last.next != in a customer support center when a support agent handles calls

#1. if a new urgent call comes it is handled after the current call finishes

 #2. the most recent call is completed first
 #create a program to stimulate a call handling system using stack. The system should allow

#a. recieve a call

#b. end the current call(pop the top caller id from the stack)

#c. display which call has ended

#d. if there are no call display the message

#display the caller id recently been handled



#check pending calls

 dissplay the total number of calls waiting in the stackself.head:
            last = last.next

        new_node.next = self.head
        last.next = new_node
        self.head = new_node

        in a customer support center when a support agent handles calls

#1. if a new urgent call comes it is handled after the current call finishes

 #2. the most recent call is completed first
 #create a program to stimulate a call handling system using stack. The system should allow

#a. recieve a call

#b. end the current call(pop the top caller id from the stack)

#c. display which call has ended

#d. if there are no call display the message

#display the caller id recently been handled



#check pending calls

 dissplay the total number of calls waiting in the stack


